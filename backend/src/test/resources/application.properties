# Testcontainers - Spring Boot가 자동으로 DB 연결 설정을 컨테이너에 맞게 잡아줌
spring.datasource.url=${TEST_DB_URL}
spring.datasource.driver-class-name=${TEST_DB_DRIVER}

# JPA
spring.jpa.hibernate.ddl-auto=${TEST_JPA_DDL_AUTO}