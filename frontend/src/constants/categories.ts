/**
 * 공지 카테고리 관련 상수
 * 대분류 태그 순서 및 소분류 태그 기본 목록 정의
 *
 * [정책 5/13] 대분류(카테고리)와 소분류(태그)는 독립적으로 동작한다.
 * 소분류 태그는 대분류의 하위 개념이 아니라, 사용자 정의 태그 기반이다.
 */

import type { NoticeCategory } from '@/src/types/api';

/** 대분류 탭 순서 (기본값) */
export const CATEGORY_TABS: NoticeCategory[] = ['일반', '장학', '학사', '취창업'];

/**
 * 소분류 태그 목록 — 대분류와 독립적으로 동작
 * 추후 사용자 정의 태그가 이 목록에 추가됨
 */
export const TAG_LIST: string[] = [
  '#국가장학',
  '#교내장학',
  '#근로장학생',
  '#등록금',
  '#외부장학',
  '#수강신청',
  '#졸업',
  '#성적',
  '#교환학생',
  '#취업',
  '#인턴십',
  '#창업',
  '#공모전',
  '#학사일정',
  '#행사',
];
